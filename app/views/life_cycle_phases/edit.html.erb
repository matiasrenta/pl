<%= render "shared/error_messages", :target => @life_cycle_phase %>

<div class="controls">
<% if can? :read, @life_cycle_phase %>
<%= link_to t("helpers.link.show"), @life_cycle_phase %>
<% end %>
<%= link_to t("helpers.link.list"), life_cycle_phases_path %>
</div>
<div class="clear"></div>

<%= render 'form' %>

<div class="controls">
<% if can? :read, @life_cycle_phase %>
<%= link_to t("helpers.link.show"), @life_cycle_phase %>
<% end %>
<%= link_to t("helpers.link.list"), life_cycle_phases_path %>
</div>