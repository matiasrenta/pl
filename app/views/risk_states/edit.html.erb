<%= render "shared/error_messages", :target => @risk_state %>

<div class="controls">
<% if can? :read, @risk_state %>
<%= link_to t("helpers.link.show"), @risk_state %>
<% end %>
<%= link_to t("helpers.link.list"), risk_states_path %>
</div>
<div class="clear"></div>

<%= render 'form' %>

<div class="controls">
<% if can? :read, @risk_state %>
<%= link_to t("helpers.link.show"), @risk_state %>
<% end %>
<%= link_to t("helpers.link.list"), risk_states_path %>
</div>