<%= render "shared/error_messages", :target => @company_type %>

<div class="controls">
<% if can? :read, @company_type %>
<%= link_to t("helpers.link.show"), @company_type %>
<% end %>
<%= link_to t("helpers.link.list"), company_types_path %>
</div>
<div class="clear"></div>

<%= render 'form' %>

<div class="controls">
<% if can? :read, @company_type %>
<%= link_to t("helpers.link.show"), @company_type %>
<% end %>
<%= link_to t("helpers.link.list"), company_types_path %>
</div>