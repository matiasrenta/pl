<%= render "shared/error_messages", :target => @risk_impact %>

<div class="controls">
<% if can? :read, @risk_impact %>
<%= link_to t("helpers.link.show"), @risk_impact %>
<% end %>
<%= link_to t("helpers.link.list"), risk_impacts_path %>
</div>
<div class="clear"></div>

<%= render 'form' %>

<div class="controls">
<% if can? :read, @risk_impact %>
<%= link_to t("helpers.link.show"), @risk_impact %>
<% end %>
<%= link_to t("helpers.link.list"), risk_impacts_path %>
</div>