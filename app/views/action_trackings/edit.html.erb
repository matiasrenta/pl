<%= render "shared/error_messages", :target => @action_tracking %>

<div class="controls">
  <% if can? :read, @action_tracking %>
      <%= link_to t("helpers.link.show"), @action_tracking %>
  <% end %>
  <%= link_to t("helpers.link.list"), action_trackings_path %>
</div>
<div class="clear"></div>

<%= render 'form' %>

<div class="controls">
  <% if can? :read, @action_tracking %>
      <%= link_to t("helpers.link.show"), @action_tracking %>
  <% end %>
  <%= link_to t("helpers.link.list"), action_trackings_path %>
</div>